# Copia este ficheiro para .env e preenche os valores reais
# NUNCA faças commit do .env para o repositório

# Chave secreta para sessões Flask — gera uma com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=coloca-aqui-uma-chave-longa-e-aleatoria

# Chave da API Football
API_FOOTBALL_KEY=coloca-aqui-a-tua-chave
